// components/features/RateProductDialog/index.ts → barrel re-export
export { default } from './RateProductDialog';
// (component is also located in ./index.tsx but TS won't see two index entries
//  in the same folder — we re-route here.)
